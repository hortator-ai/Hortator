---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clusteragentroles.core.hortator.ai
spec:
  group: core.hortator.ai
  names:
    kind: ClusterAgentRole
    listKind: ClusterAgentRoleList
    plural: clusteragentroles
    singular: clusteragentrole
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.description
      name: Description
      priority: 1
      type: string
    - jsonPath: .spec.tierAffinity
      name: Tier
      type: string
    - jsonPath: .spec.defaultModel
      name: Model
      type: string
    - jsonPath: .spec.defaultEndpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterAgentRole is the cluster-scoped Schema for agent roles
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentRoleSpec defines the desired state of AgentRole and
              ClusterAgentRole.
            properties:
              antiPatterns:
                description: |-
                  AntiPatterns is a list of things agents in this role should avoid.
                  Injected into the agent's system prompt.
                items:
                  type: string
                type: array
              apiKeyRef:
                description: ApiKeyRef is a reference to a K8s Secret containing the
                  API key.
                properties:
                  key:
                    description: Key is the key within the secret.
                    type: string
                  secretName:
                    description: SecretName is the name of the secret.
                    type: string
                required:
                - key
                - secretName
                type: object
              defaultEndpoint:
                description: DefaultEndpoint is the base URL for the LLM API.
                type: string
              defaultModel:
                description: DefaultModel is the model name (e.g. claude-sonnet-4-20250514).
                type: string
              description:
                description: |-
                  Description is a human-readable description of what this role does.
                  Used for role discovery by agents and in CLI output.
                type: string
              health:
                description: |-
                  Health defines per-role health/stuck detection overrides.
                  These sit between the cluster defaults and per-task overrides in the cascade:
                  ConfigMap defaults -> AgentRole -> AgentTask (most specific wins).
                properties:
                  stuckDetection:
                    description: StuckDetectionSpec defines stuck detection parameters.
                    properties:
                      action:
                        enum:
                        - warn
                        - kill
                        - escalate
                        type: string
                      maxRepeatedPrompts:
                        type: integer
                      statusStaleMinutes:
                        type: integer
                      toolDiversityMin:
                        type: number
                    type: object
                type: object
              rules:
                description: |-
                  Rules is a list of behavioral instructions for agents in this role.
                  Injected into the agent's system prompt.
                items:
                  type: string
                type: array
              tierAffinity:
                description: |-
                  TierAffinity is the suggested tier when this role is used (e.g. "centurion", "legionary").
                  Helps agents choose appropriate delegation targets.
                type: string
              tools:
                description: Tools is a list of tool names available to this role.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
